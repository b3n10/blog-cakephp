# Simle Blog website using CakePHP

# Reminder

To prevent showing errors from other coding standards (e.g. PEAR) and only use CakePHP, issue the command below:

```
/vendor/bin/phpcs --config-set default_standard CakePHP
```
